<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <title>Agendamento Barbearia</title>
  <!-- Tailwind via CDN (pra estilizar fácil) -->
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex items-center justify-center h-screen">
  <div class="bg-white p-8 rounded-2xl shadow-lg w-96">
    <h1 class="text-2xl font-bold mb-4 text-center">Agendar Horário</h1>
    <form action="/agendar" method="post" class="space-y-4">
      <!-- Nome -->
      <input type="text" name="nome" placeholder="Seu nome" required
        class="w-full p-2 border rounded-lg" />

      <!-- Data -->
      <input type="date" name="data" required
        class="w-full p-2 border rounded-lg" />

      <!-- Lista de horários (vem do Python pelo Jinja2) -->
      <select name="horario" class="w-full p-2 border rounded-lg" required>
        {% for h in horarios %}
          <option value="{{h}}">{{h}}</option>
        {% endfor %}
      </select>

      <!-- Botão -->
      <button type="submit"
        class="w-full bg-blue-600 text-white py-2 rounded-lg hover:bg-blue-700 transition">
        Confirmar Agendamento
      </button>
    </form>

    <a href="/agendamentos" class="block text-center mt-4 text-blue-600 hover:underline">
      Ver Agendamentos
    </a>
  </div>
</body>
</html>
